# Admin authentication (basic auth in middleware)
ADMIN_USER=admin
ADMIN_PASS=change_me_to_a_strong_password

# Where the site keeps uploads + messages.jsonl (absolute path recommended in production)
# SITE_STORAGE_ROOT=/home/i48ptgvnis/site/storage

NODE_ENV=production
